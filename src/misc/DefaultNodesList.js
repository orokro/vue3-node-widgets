/*
	DefaultNodesList.js
	-------------------

	This utility file just provides an export with the default (built-in) nodes
*/

// import our nodes to use in the list
import { NWNode } from '../classes/NWNode';

export const defaultNodesList = [

	{
		class: NWNode,
		menuPath: 'General/Node'
	}


];
